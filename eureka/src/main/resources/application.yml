spring:
  application:
    name: eurekaServer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      #自己不注册
    register-with-eureka: false
  server:
    enable-self-preservation: false
server:
  port: 8761
